// TODO(client): point this at the production domain. It resolves the OG image, the
// canonical URL and the sitemap entry, so social previews break silently if it's wrong.
export const SITE_URL = "https://planiscapital.com"
